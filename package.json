{
  "name": "z-ai-dense-graph",
  "version": "0.1.0",
  "description": "Z.ai platform of modular skills, standards, and guard rules — NOT a web application",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "eslint . --ext .ts,.tsx",
    "lint:fix": "eslint . --ext .ts,.tsx --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "check:md": "bash scripts/check-md.sh",
    "check:graph": "ts-node scripts/check-id-graph.ts",
    "validate": "npm run lint && npm run typecheck && npm run test",
    "prepare": "husky"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8.62.1",
    "@typescript-eslint/parser": "^8.62.1",
    "eslint": "^9.39.4",
    "eslint-plugin-markdown": "^5.1.0",
    "husky": "^9.1.7",
    "lint-staged": "^17.0.8",
    "prettier": "^3.9.4",
    "typescript": "^5.9.3",
    "vitest": "^2.1.9"
  },
  "lint-staged": {
    "*.{ts,tsx,js,jsx}": [
      "eslint --max-warnings=0"
    ],
    "*.md": [
      "eslint --plugin markdown --max-warnings=0"
    ]
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
