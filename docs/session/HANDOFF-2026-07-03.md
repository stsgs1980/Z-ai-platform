# Handoff: 2026-07-03

## Session Summary

Massive cleanup and restructuring of Z-ai-skills into Z-ai-platform monorepo.

## What Was Done

### 1. Skills Cleanup (37 → 14)

**Archived (15):**
- api-retry, commit-work, context-consolidation, database-schema-designer, fallback, folder-indexer, health-check, humanizer, memory-delete, memory-export, memory-query, memory-store, phi-layout (old), react-dev, reducing-entropy, requirements-clarity

**Deleted (2):**
- dev-watchdog (duplicate of sandbox-rules), gepetto (planning automation not needed)

**Replaced (1):**
- skill-creator → zai-skill-creator (from Desktop SKILLSET LIBRARY)

**Added (1):**
- zai-md-std (Markdown Standard compliance)

**Session skills removed (3):**
- zai-session-experience, zai-session-handoff, zai-session-log (ChromaDB dependencies, not compatible with sandbox)

**Sync toolkit archived (1):**
- zai-sync-toolkit (dead artifact, server↔Windows sync no longer needed)

**ID system merged (1):**
- zai-skill-id-system merged into zai-skill-registry

### 2. Naming Standardization

- Removed `_sts` suffix from all skills
- Added `zai-` prefix to all skills
- All skills now: `zai-<skill-name>`

### 3. Frontmatter Standardization

All 14 skills now have:
```yaml
name: zai-<skill-name>
author: StsDev
version: <version>
```

### 4. Monorepo Conversion

- Removed `skills` git submodule from Z-ai-platform
- Moved all 14 skills directly into `Z-ai-platform/skills/`
- Updated `bootstrap.sh`: `skills/skills` → `skills`
- Removed `skills/` from `.gitignore`
- Added `skills/` to `eslint.config.js` ignores
- Added `--no-warn-ignored` to lint-staged markdown config
- Updated test: `skills/` no longer in `.gitignore`

### 5. Documentation Updates

- `Z.ai-Sandbox-Guide.md`: Added Golden Rules 8-9, build verification
- `Z.ai-Sandbox-Migration Guide.md`: Added Critical Rules, checklist
- `DECISIONS_LOG.md`: Added D-011 (fact-check corrections)
- `INDEX.md`: Updated with all changes
- `worklog.md`: Added session entry

### 6. Validation Script

Created `scripts/validate-skills.ps1` — checks:
- All folders match INDEX.md
- All SKILL.md have required frontmatter (name, author, version)
- No duplicate IDs

Pre-commit hook installed at `.git/hooks/pre-commit`

## Current State

### Skills (14)

```
zai-anti-monolith/          ZAI-ARCH-002    v1.0
zai-debugging/              —               v1.0.0
zai-frontend-styling-expert/ ZAI-STS-004    v1.0
zai-md-std/                 —               v1.0.0
zai-mermaid-diagrams/       ZAI-ARCH-001    v1.0
zai-performance-code-generator/ ZAI-STS-003 v1.0
zai-phi-layout/             ZAI-STS-005    v3.0
zai-project-clone/          ZAI-DEV-001    v1.0
zai-prompt-engineering/     ZAI-STS-001    v1.1
zai-sandbox-rules/          —               v1.5.0
zai-skill-creator/          ZAI-STS-008    v1.0
zai-skill-registry/         ZAI-META-002   v1.2
zai-ui-composer/            ZAI-STS-006    v1.1.2
zai-workflow-discipline/    ZAI-STS-007    v2.0
```

### File Structure

```
My Projects/
├── Z-ai-platform/
│   ├── skills/           ← 14 skills (monorepo)
│   ├── guard/            ← submodule
│   ├── standards/        ← submodule
│   ├── bootstrap.sh      ← reads skills/
│   └── ...
└── Zai modules/          ← TO DELETE manually
```

### Key Files

- `skills/INDEX.md` — catalog
- `scripts/validate-skills.ps1` — validation
- `bootstrap.sh` — sandbox setup
- `AGENT_RULES.md` — agent onboarding

## Pending

1. **Delete `Zai modules`** — Windows can't delete (long paths in guard/node_modules). User needs to delete manually via Explorer.

2. **DOC-002 v2.3.2 §8** — skill scope exclusion reversal (separate task)

3. **Git force-push prohibition** — user remembers some git commands were prohibited but can't recall exactly (DEFERRED)

## Environment

- **Z.ai sandbox**: Skills via bootstrap.sh symlinks
- **Zcode**: Skills via `~/.zcode/skills/` (same SKILL.md format)
- **OpenCode**: Different format, not compatible

## Next Session Start

```bash
# Verify skills
pwsh scripts/validate-skills.ps1

# Check git status
git status

# Run tests
npm test
```
